# Terraform

Future infrastructure-as-code home.

Do not add Terraform until the Railway, Vercel, Supabase, Redis, and object storage decisions are
stable enough to avoid churn. The current source of truth is the docs and service descriptors in
`infrastructure/railway`, `infrastructure/vercel`, and `infrastructure/docker`.
