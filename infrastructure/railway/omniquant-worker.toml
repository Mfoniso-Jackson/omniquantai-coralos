[build]
builder = "DOCKERFILE"
dockerfilePath = "infrastructure/docker/worker.Dockerfile"

[deploy]
startCommand = "npm run worker --prefix examples/marketplace/feed"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
NODE_ENV = "production"
CORAL_NAMESPACE = "omniquant"
